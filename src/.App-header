#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);
    string s; 
    cin >> s; 
    int d; 
    cout << (int) s.size() - count(s.begin(), s.end(), '1') + 2 * (count(s.begin(), s.end(), '0')) << "\n"; 
    return 0; 
}